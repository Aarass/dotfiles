set -ag terminal-overrides ",$TERM:RGB"

set -sg escape-time 0

set -g mouse on
set -g base-index 1
set -g renumber-windows on

unbind C-b
set-option -g prefix C-space
bind-key C-space send-prefix

bind Space last-window


set-option -g status off
bind f set status

set-window-option -g mode-keys vi
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'

bind k select-pane -U
bind j select-pane -D
bind h select-pane -L
bind l select-pane -R


# bind-key p run-shell "~/dotfiles/scripts/posting.tmux.sh"
# bind-key g run-shell "~/dotfiles/scripts/lazygit.tmux.sh"
# bind-key y run-shell "~/dotfiles/scripts/lazydocker.tmux.sh"
# bind-key r run-shell "~/dotfiles/scripts/run.tmux.sh"
# bind-key e run-shell "~/dotfiles/scripts/edit-run.tmux.sh"
# bind-key t run-shell "script -q -c '~/dotfiles/scripts/tools.sh' /dev/null"
#
# bind-key S run-shell "~/dotfiles/scripts/replace-session.sh"

# # List of plugins
# set -g @plugin 'tmux-plugins/tpm'
# set -g @plugin 'tmux-plugins/tmux-resurrect'
# source-file ~/dotfiles/plugins/tmux/waybar.tmux.conf
#
# # Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
# run '~/.tmux/plugins/tpm/tpm'
